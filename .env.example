# Groq (client-side Vite env)
# NOTE: Any VITE_ variable is embedded into the browser build.
# If you need a real secret, call Groq from a backend/serverless function instead.

VITE_GROQ_API_KEY=
VITE_GROQ_MODEL=groq/compound
